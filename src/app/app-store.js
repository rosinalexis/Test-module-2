import { createStore } from "vuex";
import { ProfileStore } from "@/app/profile/store";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    ProfileStore,
  },
});
